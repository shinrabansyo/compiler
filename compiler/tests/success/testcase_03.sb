
fn main() {
    const value_1: i32 = 10;
    const value_2: i32 = 20;
    const value_3: i32 = 30;
    const sum: i32 = value_1 + value_2 + value_3;
    return assert_eq(sum, 60);
}

fn assert_eq(result: i32, expect: i32) {
    return result - expect;
}
