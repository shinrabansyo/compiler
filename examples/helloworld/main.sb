fn main() {
    out(0, 72);  // 'h'
    out(0, 101); // 'e'
    out(0, 108); // 'l'
    out(0, 108); // 'l'
    out(0, 111); // 'o'
    out(0, 44);  // ','
    out(0, 32);  // ' '
    out(0, 87);  // 'W'
    out(0, 114); // 'o'
    out(0, 111); // 'r'
    out(0, 108); // 'l'
    out(0, 100); // 'd'
    out(0, 33);  // '!'
    out(0, 10);  // '\n'
    return 0;
}
